```
# Navigate to project root (not backend subfolder)
cd C:\Users\shrik\OneDrive\Desktop\pro2

# Check current status
git status

# Add all changes from root directory
git add .

# Commit everything
git commit -m "Update backend for production deployment and frontend for APK build"

# Force push to overwrite remote
git push origin main --force
```